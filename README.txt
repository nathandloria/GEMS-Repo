Compile and Execute the project:
----------------------
cd gems-src
./build
./run /Users/amohan/Desktop/teaching/2019-2020/gems/gems-tester/
"the path in the above command refers to the application that is being compiled and generate error diagnostic details." 

-------------------------------------------------------------------------------
Introduce some errors in the Java file inside gems-tester/src directory. Then run the program, which would lead to an error file produced inside the gems-tester directory called compiler-errors.txt file. 
-------------------------------------------------------------------------------
